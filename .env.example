# === Required ===
JIRA_BASE_URL=https://your-org.atlassian.net
JIRA_API_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token
JIRA_PROJECT_KEY=PROJ
JIRA_BOARD_ID=1
CRON_SECRET=your-random-secret-for-cron-auth

# === Database (auto-set by Vercel Postgres integration) ===
# POSTGRES_URL=postgresql://...

# === Optional: Notifications ===
# RESEND_API_KEY=re_...
# REPORT_EMAIL_TO=team@example.com
# REPORT_EMAIL_FROM=dashboard@resend.dev
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# === Optional: Tuning ===
# REPORT_DAY=1              # 0=Sun, 1=Mon (default)
# WIP_LIMIT=3               # Per-assignee WIP cap
# STALE_DAYS=60              # Days before flagging stale
# ZOMBIE_DAYS=90             # Days with no status change
# SPRINT_ISR_TTL=300         # ISR revalidation for sprint data (seconds)
# BACKLOG_ISR_TTL=1800       # ISR revalidation for backlog data (seconds)
# STORY_POINTS_FIELD=customfield_10016  # Auto-discovered if not set
